# v3.1 Requirement → Implementation Gap Matrix

日期：2026-09-15（Asia/Taipei）
基线：v3.1 architecture/task-list；不新增 P0 原则；T13 明确不在范围内。
Closure baseline：`bfde9ffbe`；v3.1 implementation/test evidence：`3ee22da86`、
`ceb796e6b`、`14da4fc22`、`4eacafe55`

This matrix is the controlled entry point for the v3.1 delta. Existing v3.0
contracts remain authoritative; a row is not PASS merely because its core
package can be imported. It needs executable tests, bounded re-entry coverage
and evidence tied to a commit.

| Requirement | Task | Existing overlap | Gap at v3.1 start | Implementation | Test | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Run phases are exploring → planning → acting; only acting may use declared writes; simple tasks may fast-path | T3.2-A | `TaskPermissions`, `authorizeWorkerExecution`, `PiWorker`, `LoopBudgetController` | No phase state, retryable non-acting write denial or deterministic fast path | `permission-phases.ts`: monotonic phases, read-only Explore/Plan, Act ceiling, fast path | `permission-phases.test.ts` (3) | B9; `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Worker evidence must cover real success/failure/timeout/INSUFFICIENT_CONTEXT, idempotency and Work Receipt | v3.0 T3.2–T3.5 closure | Result schema, receipt validation, local process E2 fixtures | Five real Worker tasks and provider-backed evidence unavailable | Existing code; real evidence remains external | Existing local tests | `phase-03.md`, real-provider blocker | BLOCKED_EXTERNAL |
| Lifecycle hooks are deterministic scripts/static checks; missing checker warns with Evidence | T4.2-B | Pipeline stages and verification command runner | No hook registry or pre/post/cwd boundary | `lifecycle-hooks.ts` plus pipeline pre/post/risk integration | `lifecycle-hooks.test.ts`, `v3.1-pipeline-controls.test.ts` | B10; `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Context resolves org → project → directory; same-level conflict blocks; legacy exception explicit | T6.2-A | `ContextStore`, `ContextResolver`, `ContextManifest` | No scoped precedence/conflict/legacy rule layer | `scoped-context.ts`: coarse-to-fine override, exclusions, legacy-only rule | `scoped-context.test.ts` (3) | `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Completed task memory is summarized, decisions compacted, raw Evidence archived, no-op is legal | T7.5 | `TriggerGateway`, `TaskContract`, `WorkReceipt`, `EvidenceRecord` | No consolidation cycle, cold archive or hot-path token delta | `memory-consolidation.ts`: webhook/schedule trigger, hot compact memory, cold archive, no-op | `memory-consolidation.test.ts` (4) | B11; `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Real full E2E and self-development must be proven; no Mock substitution | v3.0 T7.1–T7.4 closure | Three local child-process E2 paths and trigger/routine tests | Real self-development and long-running real pipeline unavailable | Existing local E2 only | Existing local E2 tests | `phase-7-real-e2e.test.ts` | BLOCKED_EXTERNAL |
| Every shell command is safe/risky/danger after Task whitelist; risky is approval-bound, danger permanent block | T10.4 | Task shell allowlist and `evaluateTaskPermissions` | No declarative risk classifier or digest/revision approval | `command-risk.ts`: precedence, whitelist-first, digest/revision-bound approval | `command-risk.test.ts`, pipeline control tests | `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Default tool set is narrow; Bash is fallback; review checklist and replacement baseline exist | T10.5 | `TaskExecution.allowed_tools`, Evidence command list | No tool exposure policy or Bash replacement measurement | `progressive-tools.ts`: 9 built-ins, review checklist, Bash fallback and baseline | `progressive-tools.test.ts` (5) | `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Progressive tools expose <20 built-ins, then capability-matched extensions/MCP, remote last | T12.1–T12.3 | `WorkerPluginManifest`, `WorkerRegistry`, capability tags | Registry exposure currently not progressive | Capability-tag expansion with builtin → extension → MCP → remote ordering | `progressive-tools.test.ts` | B12; `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Cross-Worker messages are structured readiness notifications only; Controller rechecks state/artifact readiness | T12.1–T12.3 clarification | Artifact handoff contracts and persistent state | No bounded notice schema or wakeup recheck | Strict `HANDOFF_READY` schema; message only wakes Controller | `progressive-tools.test.ts` | B12; `evidence/v3.1-runtime-controls-2026-09-15.md` | PASS |
| Two Worker kinds, same-budget Single/Multi benchmark, pool lease/429/500/cache clear evidence | v3.0 T12 closure | Registry, pool, provider resilience, cache and local benchmark | Real external Worker/provider authorization unavailable | Existing local kind only | Existing local tests | `phase-12.md`, blocker | BLOCKED_EXTERNAL |

## v3.1 acceptance rule

The six new implementation rows and the T12 clarification are `PASS` at the
bounded local contract/test level because code, Evidence references and Bound
Coverage are present. The real-provider rows remain `BLOCKED_EXTERNAL`: local
adapters and synthetic results cannot promote five real Worker tasks, T7.2
self-development, long-running trigger evidence, or two independently
validated Worker backends. The aggregate root command also retains the
inherited client entry-resolution baseline failure. Therefore this matrix is
not a T13 promotion decision.

```text
NOT READY FOR T13
STOP; T13 not entered
```

## External Evidence Closure update — 2026-09-15

The controlled inventory is the evidence source for the still-open real-runtime
rows: [`external-worker-inventory-2026-09-15.md`](./evidence/external-worker-inventory-2026-09-15.md).
It records the exact adapter, quota, authentication and second-backend blockers
without treating package presence, static manifests, callback fixtures or local
processes as real Provider evidence. No status in the matrix is promoted by
this inventory; the real-provider rows remain `BLOCKED_EXTERNAL`.

## External Evidence Closure update — 2026-09-15 (latest)

The real-runtime rows now distinguish the first validated backend from the
still-open heterogeneous requirement:

| Requirement | Latest evidence | Status |
| --- | --- | --- |
| Five real Worker cases with Result/Receipt/Verification | `evidence/phase-03-real-worker-2026-09-15.json`; PI Agent + exact observed DeepSeek route; 5/5 conformance semantics | `PASS_REAL_WORKER_TYPE_A` |
| Real full E2E, self-development, Trigger/Routine/Memory | `evidence/phase-07-real-worker-2026-09-15.json`; 3/3 E2E after one bounded follow-up, detached self-development patch and controlled accelerated cycle | `PASS_REAL_TYPE_A_BOUNDED` |
| Two Worker kinds and same-budget heterogeneous Single/Multi | Codex process probe exists, but runtime model is `unknown` and adapter probe hit fixed CLI context overhead; no second conformance or benchmark | `BLOCKED_EXTERNAL` |

The Type A provider evidence is limited to synthetic/public conformance input;
it does not widen the internal Provider allowlist. Local 429/500, pool/cache,
and same-kind benchmark results remain local evidence. The aggregate inherited
client entry-resolution baseline and exact `google-shared.ts:402` diagnostic
also remain visible. **NOT READY FOR T13; T13 not entered.**

## Latest Type B and heterogeneous evidence — 2026-09-15

The prior row is retained as the historical pre-enable state. The latest
machine evidence is
[`evidence/phase-12-real-heterogeneous-2026-09-15.json`](./evidence/phase-12-real-heterogeneous-2026-09-15.json):

| Requirement | Latest evidence | Formal status |
| --- | --- | --- |
| Two distinct Worker kinds | Registry has two candidates stable across three reads; `pi` selects `pi-agent` and `codex` selects `codex-cli` | `PASS_EXECUTION_MODEL_IDENTITY_UNKNOWN` |
| Type B conformance | Local adapter suite `10/10` PASS; real Codex smoke `DONE`/success/independent verification PASS with valid no-op Receipt | `PASS_EXECUTION_MODEL_IDENTITY_UNKNOWN` |
| Same-budget Single/Multi | Type A single `5/5`; heterogeneous `A/B/A/B/A` `5/5`; same five tasks, permissions, tools and loop semantics | `PASS_EXECUTION_MODEL_IDENTITY_UNKNOWN` |
| Provider/model identity | Codex binary/version/session evidence exists; `platform_accepted_model`, `observed_runtime_model` and provider echo are `unknown` | `BLOCKED_EXTERNAL` for release closure |

The Type A evidence remains synthetic/public-only and continues not to widen
the internal Provider allowlist. The Type B fixed overhead is measured
separately (`29705` provider input tokens); PPH projected budget is `1500`,
effective provider ceiling `31205`, and benchmark projected Type B input is
`988`. Cost is unavailable for Type B and therefore for the heterogeneous
per-task cost metric. Local pool/429/500/cache and Bound Coverage evidence is
revalidated but is not upgraded to production or Provider claims. The matrix
remains **NOT READY FOR T13; T13 not entered**.

## Codex Type B Model Identity Attestation Closure — 2026-09-15

The historical Type B benchmark record is retained unchanged. The bounded
identity investigation is recorded in
[`evidence/type-b-identity-attestation-2026-09-15.json`](./evidence/type-b-identity-attestation-2026-09-15.json).

| Requirement | Closure evidence | Formal status |
| --- | --- | --- |
| Requested/accepted/observed identity separation | Real five-event CLI JSONL probe; no accepted or observed model/provider fields emitted | `BLOCKED_EXTERNAL` |
| Same-run identity binding | Hashed thread binding exists, but no identity field occurs in that stream; desktop/shared logs are not the Type B process | `BLOCKED_EXTERNAL` |
| Supported CLI/session fallback | Public schema is configured/latest state; internal session-config event was not emitted or persisted for this ephemeral run | `BLOCKED_EXTERNAL` |
| Adapter action | No inference patch; existing fail-closed behavior preserved | `NO_SOURCE_CHANGE` |

The resulting tuple is `gpt-5.6-sol / unknown / unknown / unknown` for
requested / platform accepted / observed runtime / provider. The previous
`PASS_EXECUTION_MODEL_IDENTITY_UNKNOWN` Phase 12 result is not overwritten;
the identity exit remains blocked. **NOT READY FOR T13; T13 not entered.**

## Identity surface compatibility closure — 2026-09-15 latest

The machine record is
[`evidence/type-b-identity-surface-compatibility-2026-09-15.json`](./evidence/type-b-identity-surface-compatibility-2026-09-15.json).
The official stable 0.154.0 binary, the isolated official npm
`0.155.0-alpha.3.10` candidate, and the installed desktop
`0.154.0-alpha.6.2` bundle were checked. The candidate schemas preserve
configured/latest `Thread.model`/`Thread.modelProvider` state and add no
machine-readable `SessionConfigured` event. Conditional model notifications
are reroute/safety-buffering only and omit provider, so they do not form a
per-turn accepted/observed identity event. The alpha real isolated run emitted
ten JSONL events with no identity;
the app-server/debug surfaces did not provide a permitted identity contract.

| Requirement | Latest closure result | Status |
| --- | --- | --- |
| Official newer candidate | 0.155.0-alpha.3.10 isolated; no identity schema or run field | `BLOCKED_EXTERNAL` |
| Same-run model/provider binding | No `SessionConfigured`/equivalent event; Thread fields are non-telemetry | `BLOCKED_EXTERNAL` |
| Adapter action | No inference patch; missing identity remains fail-closed; targeted adapter 10/10 and regression checks pass | `NO_SOURCE_CHANGE` |

The previous `PASS_EXECUTION_MODEL_IDENTITY_UNKNOWN` record and the
`A/B/A/B/A` benchmark remain unchanged. **NOT READY FOR T13; T13 not entered.**
