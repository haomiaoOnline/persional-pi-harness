# v3.1 Delta Audit

日期：2026-09-15（Asia/Taipei）
仓库：`/Users/chenglong/github/persional-pi-harness`
分支：`feature/t1-task-contract`
审计起点：`bfde9ffbe`
v3.1 implementation/test commits：`3ee22da86`、`ceb796e6b`、`14da4fc22`、`4eacafe55`
范围：v3.0 Closure remaining → v3.1 delta through T12；明确不进入 T13。

## Operating decision

The v3.0 coding-agent differential is closed: 76 PPH-only failures were
targeted as identity fixture drift and the formal suite is green. The inherited
upstream TypeScript diagnostic remains visible to `npm run check`, but the
explicit Known-Upstream-Failure gate now requires an exact fingerprint,
pinned-source evidence, pristine reproduction and PPH proof suite before a
normal commit can proceed. The upstream source is unchanged.

The strict v3.0 real-runtime gaps remain open: no authorized real external
Provider/credential path is available for five real Worker tasks, a real
self-development run, or two independently validated Worker backends. Local
child processes are retained as E2 evidence only. Two native Worker attempts
were tried under the permitted internal route, but both stopped at the
account usage limit before producing an auditable result; no output was
adopted and the runtime model was therefore `unknown`.

## Requirement matrix

The live requirement-to-gap record is [`v3.1-gap-matrix.md`](./v3.1-gap-matrix.md).
The baseline `NOT_STARTED` rows were implemented and tested in four focused
commits. The six v3.1 implementation tasks and the T12 structured message
clarification are now `PASS` at the bounded local contract/test level; the
real-provider rows remain `BLOCKED_EXTERNAL`.

## Stage boundaries

| Area | v3.0 closure result | v3.1 delta result at audit start |
| --- | --- | --- |
| Phase 0 | PPH-only unexplained failures: 0; exact upstream gate: ALLOW | Regression confirmation required after v3.1 changes |
| Phase 1–2 | Local core PASS | Regression confirmation required |
| Phase 3 | Local Worker/Result/Receipt PASS; strict real Worker evidence missing | T3.2-A not started; real-provider blocker remains |
| Phase 4–6 | Local verifier/context PASS | T4.2-B/T6.2-A not started |
| Phase 7 | Three local E2 E2E paths PASS; real self-development/long-running path missing | T7.5 not started; strict real evidence remains blocked |
| Phase 8–11 | Local graph/recovery/security/trace PASS | Bound Coverage delta required for new re-entry paths |
| Phase 12 | Local same-kind benchmark PASS; real two-backend evidence missing | T10.5/T12.1–T12.3 clarification not started |

## Non-negotiable controls

- Task Contract remains the permission ceiling; phase escalation is retryable
  and cannot widen declared scope.
- Hooks are deterministic and fail closed for the current tool call; a missing
  checker is warning plus Evidence, not silent success or infrastructure crash.
- Context conflicts at the same scope block; precedence is explicit and
  directory legacy exceptions are testable.
- Consolidation never deletes raw Evidence and a no-new-content cycle is a
  recorded legal no-op.
- `danger` commands remain permanently blocked even when Task permissions or
  a user approval would otherwise allow them.
- Progressive tool exposure is capability-driven and ordered with remote tools
  last. Worker messages can wake the Controller but cannot carry state truth.
- No T13 files, batch implementation or parallel merge path is introduced.

## Evidence and verdict at audit start

The Phase 0 evidence is in
[`evidence/phase-00-pph-identity-closure-2026-09-14.md`](./evidence/phase-00-pph-identity-closure-2026-09-14.md).
The generated gate Decision/Evidence is local under
`.git/pph-known-upstream-failure-gate/` and is not a tracked artifact.

This is a v3.1 delta baseline, not a final release verdict:

```text
NOT READY FOR T13
v3.1 delta implementation pending
real Provider/Worker evidence blockers remain
STOP; T13 not entered
```

## v3.1 completion update — 2026-09-15

### Requirement and implementation result

The delta implementation is deliberately limited to the existing Personal PI
layer. It adds `permission-phases.ts`, `lifecycle-hooks.ts`,
`scoped-context.ts`, `memory-consolidation.ts`, `command-risk.ts` and
`progressive-tools.ts`, with exports, focused tests and four new bound paths
in `V3_FEEDBACK_PATHS`. The pipeline now applies lifecycle hooks and command
risk authorization around verification commands. No T13 file, batch path,
parallel merge path or upstream source file was changed.

| Delta | Result | Evidence |
| --- | --- | --- |
| T3.2-A phased permission escalation | PASS, bounded local contract | `permission-phases.test.ts`; B9 |
| T4.2-B deterministic lifecycle hooks | PASS, pipeline-integrated local path | `lifecycle-hooks.test.ts`, `v3.1-pipeline-controls.test.ts`; B10 |
| T6.2-A scoped context assembly | PASS, bounded local resolver | `scoped-context.test.ts` |
| T7.5 background consolidation | PASS, controlled webhook/schedule cycle | `memory-consolidation.test.ts`; B11 |
| T10.4 command risk classification | PASS, whitelist-first safe/risky/danger path | `command-risk.test.ts`, pipeline control tests |
| T10.5 narrow tools and Bash fallback | PASS, checklist and sample baseline | `progressive-tools.test.ts` |
| T12.1–T12.3 progressive exposure | PASS, capability-tagged local registry view | `progressive-tools.test.ts`; B12 |
| T12 structured Worker notice | PASS, readiness-only wakeup contract | `progressive-tools.test.ts`; B12 |

### Verification result

The current Personal PI core is `32` test files / `173` tests PASS; build and
protocol isolation PASS. Script tests are `29/29` PASS and the Personal PI
regression suite is `3` files / `23` tests PASS. The standalone isolated
coding-agent suite is `266/273` files and `2239/2293` tests PASS. A fresh
isolated `./test.sh` aggregate run retains one inherited client entry
resolution failure at `packages/client/test/unix.test.ts` while the agent,
chord, server, telemetry, coding-agent and Personal PI package results are
otherwise recorded in the evidence file.

`npm run check` still surfaces the exact inherited
`google-shared.ts:402` TS2322. The normal pre-commit hook invokes the exact
fail-closed Known-Upstream-Failure gate; it reproduced the pinned pristine
upstream failure and generated a Decision/Evidence record only after PPH
build, targeted, regression and protocol proofs passed. No `--no-verify` was
used for the v3.1 commits.

### Strict blockers and final decision

The required five real Worker tasks, real Provider credential path, T7.2
disposable self-development run, long-running real Trigger/Routine pipeline,
and two independently validated Worker kinds remain unavailable. The local
three-child-process E2E and same-kind Single/Multi benchmark remain clearly
classified as local evidence. The root aggregate client baseline failure also
prevents a repository-wide green result. These are not converted to PASS by
the local contract tests.

```text
NOT READY FOR T13
exact blockers remain; STOP
T13 was not entered
```
